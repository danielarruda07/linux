DOCKERFILE - INSTALAÇÃO DO NODEJS

  GNU nano 6.2                                                                                           dockerfile                                                                                                     
# Use uma imagem base do Node.js
FROM node:latest

# Crie e defina o diretório de trabalho no contêiner
WORKDIR /usr/src/app

# Atualize o npm para a versão mais recente
RUN npm install -g npm@latest

# Atualize o npm para a versão mais recente
RUN npm install -g npm@latest

# Instale o vim usando apt-get
RUN apt-get update && apt-get install -y vim

# Copie o restante dos arquivos do aplicativo para o diretório de trabalho no contêiner
COPY . .

# Comando para iniciar o aplicativo quando o contêiner for iniciado
CMD ["node", "app.js"]



CONTEÚDO PACKAGE.JSON

{
  "name": "meu-app-nodejs",
  "version": "1.0.0",
  "description": "Meu aplicativo Node.js",
  "main": "app.js",
  "dependencies": {
    "express": "^4.17.1" // Por exemplo, vamos adicionar o Express como uma dependência
  }
}


CONTEÚDO APP.JS

  GNU nano 6.2                                                                                             app.js                                                                                                       
const http = require('http');

const hostname = '0.0.0.0';
const port = 4000;

const server = http.createServer((req, res) => {
  res.statusCode = 200;
  res.setHeader('Content-Type', 'text/plain');
  res.end('ARROZ IDTECH DANIEL IVAN\n');
});

server.listen(port, hostname, () => {
  console.log(`Server running at http://${hostname}:${port}/`);
});

